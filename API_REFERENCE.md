# stm32_load_pwm — справочник по API

Полный список публичных функций и типов. Для быстрого повторения архитектуры - см. `README.md`,
за обоснованиями конкретных решений - комментарии в `stm32_load_pwm.h` (справочник ниже - их
сжатый пересказ, при расхождениях ориентируйтесь на `stm32_load_pwm.h`, он первичен).

## Оглавление

- [Конфигурационные define-ы (Flash/RAM)](#конфигурационные-define-ы-flashram)
- [Типы](#типы)
- [Циклы индикации](#циклы-индикации)
- [Регистрация нагрузки](#регистрация-нагрузки)
- [Фоновая яркость](#фоновая-яркость)
- [Управление циклами индикации](#управление-циклами-индикации)
- [Глобальный множитель яркости](#глобальный-множитель-яркости)
- [Периодическое обновление](#периодическое-обновление)

---

## Конфигурационные define-ы (Flash/RAM)

Задаются через `#define` ДО включения `stm32_load_pwm.h`. Подробные оценки памяти - см.
`README.md`, раздел "Настройка памяти".

| Define | По умолчанию | Описание |
|---|---|---|
| `LOAD_PWM_MAX_LOADS` | `16` | максимум одновременно зарегистрированных нагрузок (LED и LINEAR вместе) |
| `LOAD_PWM_MAX_LEDS` | `= LOAD_PWM_MAX_LOADS` | максимум именно LED-нагрузок - под них отдельный пул таблиц гамма-коррекции; задайте меньше `LOAD_PWM_MAX_LOADS`, если LED не все нагрузки |
| `LOAD_PWM_TABLE_QUALITY` | `2` (HIGH) | `0`=LOW (32 точки/цикл), `1`=MEDIUM (64), `2`=HIGH (128) - разрешение таблиц формы циклов, влияет на Flash |
| `LOAD_PWM_RAM_QUALITY` | `2` (HIGH) | `0`=LOW (`uint8_t`, 8 точек LUT), `1`=MEDIUM (`uint16_t`, 16), `2`=HIGH (`uint32_t`, 32) - тип скважности и точность гамма-кривой, влияет на RAM |
| `LOAD_PWM_DEFAULT_LED_GAMMA` | `2.2f` | типовая gamma для LED, если `brightness_mid_percent < 0` |

Некорректные значения (`LOAD_PWM_MAX_LOADS == 0`, `LOAD_PWM_MAX_LEDS >= 255`,
`LOAD_PWM_TABLE_QUALITY`/`LOAD_PWM_RAM_QUALITY` вне `{0,1,2}`) ловятся `#error` на этапе
препроцессора - опечатка в значении define не пройдёт незамеченной в билд.

---

## Типы

### `LOAD_PWM_Duty_t`

Тип хранения скважности ШИМ - `uint8_t`/`uint16_t`/`uint32_t` в зависимости от
`LOAD_PWM_RAM_QUALITY`. Ограничивает максимальное число ступеней ШИМ до предела этого типа
независимо от реального ARR таймера.

### `LOAD_PWM_Type_t`

| Значение | Описание |
|---|---|
| `LOAD_PWM_TYPE_LED` | светодиод - применяется гамма-коррекция (таблица - в отдельном пуле `LOAD_PWM_MAX_LEDS`) |
| `LOAD_PWM_TYPE_LINEAR` | линейная нагрузка (лампа накаливания, кулер, DC-мотор, электромагнит) - без коррекции, без LUT вообще |

### `LOAD_PWM_Config_t`

| Поле | Тип | Описание |
|---|---|---|
| `tick_source_id` | `uint32_t` | произвольный идентификатор "источника тика" - все нагрузки с одинаковым значением обновляются одним вызовом `LOAD_PWM_Tick()` |
| `tick_freq_hz` | `uint32_t` | заявленная частота вызовов `LOAD_PWM_Tick()` с этим `tick_source_id`, Гц |
| `htim` | `TIM_HandleTypeDef*` | таймер ШИМ-канала этой нагрузки |
| `channel` | `uint32_t` | канал таймера (`TIM_CHANNEL_1` и т.п.) |
| `load_type` | `LOAD_PWM_Type_t` | тип нагрузки - LED (гамма) или LINEAR (без коррекции) |
| `brightness_min_percent` | `float` | скважность ШИМ (0..100%), нижняя граница диапазона |
| `brightness_mid_percent` | `float` | только для LED: скважность на визуальных ~50% (измеренная точка); **отрицательное значение** = использовать типовую gamma вместо измерения. Для LINEAR - игнорируется |
| `brightness_max_percent` | `float` | скважность ШИМ (0..100%), верхняя граница диапазона |

### `LOAD_PWM_Handle_t`

Возвращается `LOAD_PWM_Init()` по указателю. Память статическая (пул на `LOAD_PWM_MAX_LOADS`
элементов, без malloc). Поля намеренно сгруппированы по размеру для минимизации паддинга
выравнивания - менять напрямую не нужно. Таблица гамма-коррекции хранится НЕ в хэндле, а в
отдельном пуле (`LOAD_PWM_MAX_LEDS` слотов) - хэндл хранит только индекс на неё
(`LOAD_PWM_LUT_INDEX_NONE`, если нагрузка типа LINEAR).

---

## Циклы индикации

`LOAD_PWM_Cycle_t` — раскладка по времени указана в долях периода. Точное число точек таблицы
каждого цикла зависит от `LOAD_PWM_TABLE_QUALITY`, но форма (раскладка по долям периода) одна
и та же на всех уровнях.

| Идентификатор | Форма |
|---|---|
| `LOAD_PWM_CYCLE_MEANDR` | 0–50% - 100% яркости, 50–100% - 0% |
| `LOAD_PWM_CYCLE_BREATH` | плавный горб (~sin) на весь период - нарастание и спад без пауз |
| `LOAD_PWM_CYCLE_PULSE` | 0–50% - горб (~sin), 50–100% - пауза |
| `LOAD_PWM_CYCLE_TRAPEZOID` | 0–10% рост, 10–40% полка на 100%, 40–50% спад, 50–100% пауза |
| `LOAD_PWM_CYCLE_SAWTOOTH` | 0–50% линейный рост, 50–100% пауза |
| `LOAD_PWM_CYCLE_TRIANGLE` | 0–50% линейный рост, 50–100% линейный спад (без паузы) |
| `LOAD_PWM_CYCLE_DOUBLE_BLINK` | 0–12.5% вкл, 12.5–37.5% выкл, 37.5–62.5% вкл, 62.5–75% выкл, 75–100% пауза |
| `LOAD_PWM_CYCLE_STROBE` | пять коротких вспышек подряд (~40% периода), затем пауза |

`LOAD_PWM_CYCLE_COUNT` — служебное значение, не валидный идентификатор.

---

## Регистрация нагрузки

### `LOAD_PWM_Handle_t *LOAD_PWM_Init(const LOAD_PWM_Config_t *config)`

Регистрирует нагрузку: пересчитывает опорные уровни из процентов в тики ШИМ (с насыщением до
предела `LOAD_PWM_Duty_t`, если ARR больше, чем этот тип способен выразить), для
`LOAD_PWM_TYPE_LED` - выделяет слот в отдельном пуле LUT и строит таблицу гамма-коррекции,
запускает `HAL_TIM_PWM_Start()`. Никакой "главный" таймер сама не запускает — см.
`LOAD_PWM_Tick()` ниже.

Возвращает указатель на хэндл либо `NULL` при ошибке (`config` некорректен, исчерпан
`LOAD_PWM_MAX_LOADS`, для LED - исчерпан `LOAD_PWM_MAX_LEDS`, либо `HAL_TIM_PWM_Start()` вернул
не `HAL_OK` - в этом случае слот только что выделенной нагрузки освобождается обратно в пул).
Повторный вызов с тем же `(htim, channel)` идемпотентен - переиспользует уже выделенный слот
LUT, если он был.

**Потокобезопасность:** если вызывается повторно для уже активной (тикающей) нагрузки не из
того же контекста, что и её `LOAD_PWM_Tick()` - оберните вызов в критическую секцию сами
(подробности - шапка `.h`).

---

## Фоновая яркость

### `HAL_StatusTypeDef LOAD_PWM_SetBrightness(LOAD_PWM_Handle_t *h, float percent, uint32_t fade_ms)`

Задаёт фоновую яркость. Переход цикл ↔ фон всегда мгновенный, независимо от `fade_ms`.
`fade_ms` управляет только плавностью перехода между фоновыми значениями, пока не запущен ни
один цикл (0 = мгновенно; >0 = плавно за указанное время). Возвращает `HAL_OK`; `HAL_ERROR`,
если `h == NULL`.

Для `LOAD_PWM_TYPE_LINEAR` `percent` преобразуется в тики ШИМ напрямую, с полным разрешением
диапазона `pwm_min..pwm_max` (независимо от `LOAD_PWM_RAM_QUALITY`). Для `LOAD_PWM_TYPE_LED`
разрешение ограничено 256 шагами - это разрешение самой интерполяции по гамма-LUT.

---

## Управление циклами индикации

### `HAL_StatusTypeDef LOAD_PWM_Start(LOAD_PWM_Handle_t *h, LOAD_PWM_Cycle_t cycle, float phase_shift, uint32_t period_ms)`

Запускает зацикленный цикл. Если тот же `cycle` уже зациклен — продолжает без сброса (параметры
игнорируются). `phase_shift` — 0.0..1.0, доля периода. `period_ms` — время одного полного
цикла, > 0.

### `HAL_StatusTypeDef LOAD_PWM_StartOnce(LOAD_PWM_Handle_t *h, LOAD_PWM_Cycle_t cycle, float phase_shift, uint32_t period_ms)`

Как `LOAD_PWM_Start`, но однократно — по завершении отключается сам, всегда стартует заново.

### `HAL_StatusTypeDef LOAD_PWM_Stop(LOAD_PWM_Handle_t *h)`

Останавливает текущий цикл, немедленно возвращает к фоновой яркости.

### `void LOAD_PWM_StopAll(void)`

Мгновенно останавливает циклы и возвращает к фону ВСЕ зарегистрированные нагрузки одним
вызовом (эквивалент `LOAD_PWM_Stop` для каждой).

---

## Глобальный множитель яркости

### `void LOAD_PWM_SetGlobalBrightness(float percent)`

Задаёт глобальный множитель (0..100%), применяемый ко всем нагрузкам сразу — удобно для
"ночного режима". Накладывается ПОСЛЕ гамма-коррекции, не искажая форму кривой; внутреннее
логическое состояние (фон, фаза цикла) множителем не затрагивается. Простаивающие нагрузки
перерисовываются немедленно; нагрузки с активным циклом/фейдом подхватят множитель на
ближайшем `LOAD_PWM_Tick()`.

---

## Периодическое обновление

### `void LOAD_PWM_Tick(uint32_t tick_source_id)`

Продвигает активные циклы и плавные переходы для всех нагрузок с указанным `tick_source_id`,
пишет новое значение ШИМ в их каналы. Вызывайте периодически с частотой, примерно равной
заявленной `tick_freq_hz` — откуда угодно:

```c
/* Вариант 1 - из прерывания таймера */
void HAL_TIM_PeriodElapsedCallback(TIM_HandleTypeDef *htim)
{
    if (htim == &htim6) { LOAD_PWM_Tick(0); }
}

/* Вариант 2 - из главного цикла */
while (1)
{
    LOAD_PWM_Tick(0);
    HAL_Delay(1); /* ~1 кГц, приблизительно */
}
```

Максимально быстрая: без malloc, без деления в основной ветке, без блокировок и без вызова
степенных/тригонометрических функций.

**Потокобезопасность:** если вызывается из прерывания, пока остальные функции API дергаются из
основного потока — см. "Честные ограничения" в `README.md` и шапку `stm32_load_pwm.h`.
