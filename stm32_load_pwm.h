/**
 ******************************************************************************
 * @file    stm32_load_pwm.h
 * @brief   Библиотека управления ШИМ-нагрузкой на STM32: циклы индикации и
 *          гамма-коррекция для светодиодов, линейный режим для прочей
 *          нагрузки (лампы накаливания, кулеры, DC-моторы, электромагниты).
 * @author  Mechanic
 * @date    18.09.2026
 * @version 0.3
 *
 * @copyright Copyright (c) 2026 Mechanic.
 *            Свободное некоммерческое использование и модификация. Условия
 *            распространения - см. LICENSE / README.md в составе проекта.
 *
 *          Поддерживаемые платформы: STM32F4, STM32H7 (и, в целом, любой
 *          STM32 с периферией TIM и HAL). Специфичного для конкретной серии
 *          кода в библиотеке нет - используется только стандартный HAL TIM.
 *
 *          === ИДЕЯ БИБЛИОТЕКИ ===
 *          Каждая нагрузка подключена к своему каналу ШИМ на своём таймере.
 *          Периодическое обновление скважности делается вызовом
 *          LOAD_PWM_Tick(tick_source_id) - библиотека НЕ привязана к
 *          конкретному аппаратному "главному" таймеру: вызывайте эту
 *          функцию откуда угодно и с какой угодно периодичностью - из
 *          прерывания таймера, из тела while(1) в main(), из задачи RTOS.
 *          tick_source_id - произвольный идентификатор для группировки
 *          нагрузок с разной частотой/источником обновления.
 *
 *          Каждая нагрузка регистрируется с явным ТИПОМ (LOAD_PWM_Type_t):
 *          LOAD_PWM_TYPE_LED (гамма-коррекция) или LOAD_PWM_TYPE_LINEAR
 *          (без коррекции - для ламп накаливания, кулеров, DC-моторов,
 *          электромагнитов и т.п.). Подробности - см. LOAD_PWM_Config_t.
 *
 *          === ПАМЯТЬ: ДВА НЕЗАВИСИМЫХ УРОВНЯ КАЧЕСТВА ===
 *          Библиотека даёт выбрать компромисс между потребляемой памятью и
 *          качеством ШИМ по ДВУМ независимым осям (см. подробные комментарии
 *          у каждого define ниже):
 *
 *          1. LOAD_PWM_TABLE_QUALITY (Flash) - разрешение таблиц формы
 *             циклов индикации (меандр/дыхание/трапеция/...). Таблицы общие
 *             для всех экземпляров - от количества нагрузок не зависит.
 *
 *          2. LOAD_PWM_RAM_QUALITY (RAM) - тип хранения скважности ШИМ
 *             (uint8_t/uint16_t/uint32_t) и разрешение таблицы гамма-
 *             коррекции для LED. Масштабируется с количеством нагрузок -
 *             см. LOAD_PWM_MAX_LOADS/LOAD_PWM_MAX_LEDS ниже.
 *
 *          Оба параметра задаются через #define ДО включения этого
 *          заголовка (например в main.h вашего проекта) - если не заданы,
 *          используются значения по умолчанию (максимальное качество, как в
 *          версии 0.1).
 *
 *          === РАЗДЕЛЬНЫЕ ПУЛЫ: НАГРУЗКИ И LED-КАЛИБРОВКА ===
 *          LOAD_PWM_MAX_LOADS - максимальное число ЛЮБЫХ зарегистрированных
 *          нагрузок (LED и линейных вместе). LOAD_PWM_MAX_LEDS - отдельно,
 *          максимальное число именно LED-нагрузок (по умолчанию равно
 *          LOAD_PWM_MAX_LOADS, т.е. "все нагрузки могут быть LED" - на
 *          всякий случай, если не переопределено).
 *
 *          Таблица гамма-коррекции НЕ хранится внутри каждого хэндла
 *          нагрузки - только для LOAD_PWM_TYPE_LED она лежит в отдельном,
 *          отдельно размеряемом пуле (LOAD_PWM_MAX_LEDS слотов). Для
 *          LOAD_PWM_TYPE_LINEAR калибровочная таблица вообще не нужна -
 *          скважность считается напрямую по точной линейной формуле. Если
 *          в проекте нагрузок больше, чем LED среди них - задайте
 *          LOAD_PWM_MAX_LEDS МЕНЬШЕ LOAD_PWM_MAX_LOADS, чтобы не платить
 *          RAM за калибровку, которая линейным нагрузкам не нужна.
 *
 *          Пример: 4 светодиода + 4 DC-мотора -> LOAD_PWM_MAX_LOADS = 8,
 *          LOAD_PWM_MAX_LEDS = 4.
 *
 *          === КАК ПОЛЬЗОВАТЬСЯ (паттерн API) ===
 *          1. В CubeMX настраиваете таймер(ы) нагрузок: канал в режиме PWM
 *             Generation, частота ШИМ - без видимого мерцания/жужжания
 *             (обычно от 1 кГц), ARR - как вам удобно.
 *          2. При необходимости - до включения заголовка задаёте
 *             LOAD_PWM_MAX_LOADS/LOAD_PWM_MAX_LEDS/LOAD_PWM_TABLE_QUALITY/
 *             LOAD_PWM_RAM_QUALITY под свой проект (см. комментарии ниже).
 *          3. Заполняете LOAD_PWM_Config_t (включая tick_source_id и
 *             tick_freq_hz) и вызываете LOAD_PWM_Init(&config) - получаете
 *             указатель на LOAD_PWM_Handle_t, храните у себя.
 *          4. Организуете периодический вызов LOAD_PWM_Tick(tick_source_id)
 *             с заявленной частотой (таймер/main-loop/RTOS - на ваш выбор).
 *          5. Управляете нагрузкой через LOAD_PWM_SetBrightness,
 *             LOAD_PWM_Start/StartOnce/Stop, LOAD_PWM_StopAll(),
 *             LOAD_PWM_SetGlobalBrightness().
 *
 *          === ПОТОКОБЕЗОПАСНОСТЬ / ВЫЗОВ ИЗ ПРЕРЫВАНИЯ ===
 *          LOAD_PWM_Tick() обычно вызывается ИЗ ПРЕРЫВАНИЯ таймера, тогда
 *          как остальные функции API (SetBrightness/Start/StartOnce/Stop/
 *          StopAll/SetGlobalBrightness/Init) обычно вызываются из основного
 *          потока/задачи RTOS. Поля хэндла, которые Tick() читает
 *          (phase_acc, mode, fade_active), объявлены volatile и внутри
 *          каждой функции API "ключевое" поле-флаг (mode/fade_active)
 *          записывается ПОСЛЕДНИМ - это защищает от чтения Tick() частично
 *          обновлённого состояния при прерывании ровно посреди записи.
 *          ОДНАКО библиотека НЕ оборачивает сама себя в критические секции
 *          (__disable_irq/__enable_irq) - если ваш проект вызывает
 *          LOAD_PWM_Init() ПОВТОРНО для уже тикающей (активной) нагрузки, из
 *          контекста ниже приоритетом, чем прерывание с Tick() для неё -
 *          оберните этот конкретный вызов в критическую секцию сами
 *          (промежуточное состояние во время самого Init() не защищено).
 *          Обычный сценарий (весь Init() - на старте, до включения
 *          прерывания с Tick()) в защите не нуждается.
 ******************************************************************************
 */

#ifndef STM32_LOAD_PWM_H
#define STM32_LOAD_PWM_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include "main.h"   /* CubeMX: тянет за собой правильный stm32xxxx_hal.h с типом
                       TIM_HandleTypeDef, используемым во всех функциях ниже */

/* ------------------------------------------------------------------------ */
/*  Размеры пулов                                                           */
/* ------------------------------------------------------------------------ */

/** Максимальное число одновременно зарегистрированных нагрузок (LED и
 *  линейных вместе). */
#ifndef LOAD_PWM_MAX_LOADS
#define LOAD_PWM_MAX_LOADS              16U
#endif

#if LOAD_PWM_MAX_LOADS == 0U
#error "LOAD_PWM_MAX_LOADS должен быть больше 0"
#endif

/** Максимальное число именно LED-нагрузок среди LOAD_PWM_MAX_LOADS - под
 *  них выделяется ОТДЕЛЬНЫЙ пул таблиц гамма-коррекции (см. описание в
 *  начале файла). По умолчанию равно LOAD_PWM_MAX_LOADS (безопасное
 *  предположение "все могут быть LED"). Если заранее известно точное число
 *  LED в проекте - задайте меньше, чтобы не платить RAM за калибровку,
 *  которая линейным нагрузкам не нужна. */
#ifndef LOAD_PWM_MAX_LEDS
#define LOAD_PWM_MAX_LEDS               LOAD_PWM_MAX_LOADS
#endif

/** Служебное значение "LUT не назначена" (нагрузка LOAD_PWM_TYPE_LINEAR,
 *  либо LED ещё не инициализирована) - требует LOAD_PWM_MAX_LEDS < 255,
 *  см. проверку ниже. */
#define LOAD_PWM_LUT_INDEX_NONE         0xFFU

#if LOAD_PWM_MAX_LEDS >= LOAD_PWM_LUT_INDEX_NONE
#error "LOAD_PWM_MAX_LEDS должен быть меньше 255 - lut_index и счётчик занятых слотов s_led_lut_pool хранятся как uint8_t, LOAD_PWM_LUT_INDEX_NONE (0xFF) зарезервировано под служебное значение"
#endif

/* ------------------------------------------------------------------------ */
/*  Качество (разрешение) таблиц формы циклов индикации - выбор Flash       */
/* ------------------------------------------------------------------------ */
/**
 * Три уровня, переопределяются через #define ДО включения этого заголовка
 * (например в main.h): 0 = LOW, 1 = MEDIUM, 2 = HIGH (умолчание, как в 0.1).
 * Таблицы ОБЩИЕ для всех экземпляров (8 циклов x N точек) - расход Flash НЕ
 * зависит от количества зарегистрированных нагрузок.
 *
 * Оценка суммарного расхода Flash (код библиотеки + все 8 таблиц циклов)
 * при сборке с -O0 (без оптимизации - консервативная, заведомо бОльшая
 * оценка; при -Os/-O2 реальный расход заметно меньше):
 *
 *   LOW    (32 точки/цикл):  ~3.1 КБ (код) + ~0.25 КБ (таблицы) = ~3.35 КБ
 *   MEDIUM (64 точки/цикл):  ~3.1 КБ (код) + ~0.5  КБ (таблицы) = ~3.6  КБ
 *   HIGH   (128 точек/цикл): ~3.1 КБ (код) + ~1.0  КБ (таблицы) = ~4.1  КБ
 *
 * (числа приблизительные, для ориентировочной оценки)
 */
#ifndef LOAD_PWM_TABLE_QUALITY
#define LOAD_PWM_TABLE_QUALITY 2
#endif

#if (LOAD_PWM_TABLE_QUALITY != 0) && (LOAD_PWM_TABLE_QUALITY != 1) && (LOAD_PWM_TABLE_QUALITY != 2)
#error "LOAD_PWM_TABLE_QUALITY должен быть 0 (LOW), 1 (MEDIUM) или 2 (HIGH)"
#endif

#if LOAD_PWM_TABLE_QUALITY == 0
#define LOAD_PWM_TABLE_BITS 5U   /* 32 точки */
#elif LOAD_PWM_TABLE_QUALITY == 1
#define LOAD_PWM_TABLE_BITS 6U   /* 64 точки */
#else
#define LOAD_PWM_TABLE_BITS 7U   /* 128 точек */
#endif
#define LOAD_PWM_TABLE_SIZE (1U << LOAD_PWM_TABLE_BITS)

/* ------------------------------------------------------------------------ */
/*  Качество (тип и разрешение) хранения скважности ШИМ - выбор RAM         */
/* ------------------------------------------------------------------------ */
/**
 * Три уровня, переопределяются через #define ДО включения этого заголовка:
 * 0 = LOW (uint8_t), 1 = MEDIUM (uint16_t), 2 = HIGH (uint32_t, умолчание,
 * как в 0.1). Влияет на тип хранения скважности (LOAD_PWM_Duty_t) И на
 * число точек интерполяции таблицы гамма-коррекции для LED.
 *
 * ВНИМАНИЕ: выбор типа ограничивает МАКСИМАЛЬНОЕ число ступеней ШИМ до
 * предела этого типа (255 / 65535 / 4294967295), НЕЗАВИСИМО от реального
 * ARR таймера - если ARR канала больше этого предела, часть аппаратного
 * разрешения останется неиспользованной (сознательный компромисс ради
 * экономии RAM, а не ошибка).
 *
 * Расход RAM МАСШТАБИРУЕТСЯ с количеством нагрузок:
 *   RAM = LOAD_PWM_MAX_LOADS * <хэндл>  +  LOAD_PWM_MAX_LEDS * <LED LUT>
 *
 * Оценка при -O0 (приблизительно, для ориентира):
 *   LOW    (uint8_t,  8 точек LUT): хэндл ~44Б  + LED LUT ~9Б
 *   MEDIUM (uint16_t, 16 точек LUT): хэндл ~48Б  + LED LUT ~34Б
 *   HIGH   (uint32_t, 32 точки LUT): хэндл ~56Б  + LED LUT ~132Б
 *
 * Пример (4 LED + 4 линейных, LOAD_PWM_MAX_LOADS=8, LOAD_PWM_MAX_LEDS=4):
 *   LOW    ~44*8 + 9*4   = ~388Б
 *   MEDIUM ~48*8 + 34*4  = ~520Б
 *   HIGH   ~56*8 + 132*4 = ~976Б
 */
#ifndef LOAD_PWM_RAM_QUALITY
#define LOAD_PWM_RAM_QUALITY 2
#endif

#if (LOAD_PWM_RAM_QUALITY != 0) && (LOAD_PWM_RAM_QUALITY != 1) && (LOAD_PWM_RAM_QUALITY != 2)
#error "LOAD_PWM_RAM_QUALITY должен быть 0 (LOW), 1 (MEDIUM) или 2 (HIGH)"
#endif

#if LOAD_PWM_RAM_QUALITY == 0
typedef uint8_t LOAD_PWM_Duty_t;
#define LOAD_PWM_GAMMA_LUT_SEGMENTS      8U
#define LOAD_PWM_GAMMA_LUT_INDEX_SHIFT   5U
#elif LOAD_PWM_RAM_QUALITY == 1
typedef uint16_t LOAD_PWM_Duty_t;
#define LOAD_PWM_GAMMA_LUT_SEGMENTS      16U
#define LOAD_PWM_GAMMA_LUT_INDEX_SHIFT   4U
#else
typedef uint32_t LOAD_PWM_Duty_t;
#define LOAD_PWM_GAMMA_LUT_SEGMENTS      32U
#define LOAD_PWM_GAMMA_LUT_INDEX_SHIFT   3U
#endif
#define LOAD_PWM_GAMMA_LUT_POINTS         (LOAD_PWM_GAMMA_LUT_SEGMENTS + 1U)
#define LOAD_PWM_GAMMA_LUT_SEGMENTS_MASK  ((1U << LOAD_PWM_GAMMA_LUT_INDEX_SHIFT) - 1U)

/** Максимальное представимое значение LOAD_PWM_Duty_t при текущем
 *  LOAD_PWM_RAM_QUALITY (255/65535/4294967295). Используется как для
 *  насыщения в LOAD_PWM_Init(), так и для безопасного клампа во float-
 *  накоплении фейда в LOAD_PWM_Tick() - см. комментарии в .c. */
#define LOAD_PWM_DUTY_MAX ((LOAD_PWM_Duty_t)~(LOAD_PWM_Duty_t)0U)

/** Типовое значение gamma для LOAD_PWM_TYPE_LED, если пользователь не
 *  измерял точку 50% (см. brightness_mid_percent ниже). Переопределяемо. */
#ifndef LOAD_PWM_DEFAULT_LED_GAMMA
#define LOAD_PWM_DEFAULT_LED_GAMMA        2.2f
#endif

/* ------------------------------------------------------------------------ */
/*  Тип нагрузки                                                            */
/* ------------------------------------------------------------------------ */

typedef enum
{
    /** Светодиод - применяется гамма-коррекция (таблица - в отдельном
     *  пуле, см. LOAD_PWM_MAX_LEDS выше). */
    LOAD_PWM_TYPE_LED = 0,

    /** Линейная нагрузка (лампа накаливания, кулер охлаждения, DC-мотор,
     *  электромагнит и т.п.) - скважность ШИМ линейна относительно
     *  заданного значения, БЕЗ гамма-коррекции и без LUT вообще. */
    LOAD_PWM_TYPE_LINEAR,
} LOAD_PWM_Type_t;

/* ------------------------------------------------------------------------ */
/*  Циклы индикации                                                         */
/* ------------------------------------------------------------------------ */
/*
 * Форма каждого цикла - готовая константная таблица в .c файле (размер
 * зависит от LOAD_PWM_TABLE_QUALITY выше), заполняются один раз на этапе
 * компиляции. Каждая таблица описывает ОДИН полный период (100% цикла).
 */
typedef enum
{
    /** Меандр: первая половина периода - максимум, вторая - ноль. */
    LOAD_PWM_CYCLE_MEANDR = 0,

    /** "Дыхание": один плавный горб на весь период - нарастание и спад
     *  (форма ~ sin по всему периоду), без пауз. */
    LOAD_PWM_CYCLE_BREATH,

    /** Импульс с ожиданием: горб (~sin) в первой половине, пауза во второй. */
    LOAD_PWM_CYCLE_PULSE,

    /** Полутрапеция: 10% рост, 30% полка на 100%, 10% спад, 50% пауза. */
    LOAD_PWM_CYCLE_TRAPEZOID,

    /** Полупила: первая половина - линейный рост 0->100%, вторая - пауза. */
    LOAD_PWM_CYCLE_SAWTOOTH,

    /** Треугольник: непрерывный линейный рост и сразу спад без паузы. */
    LOAD_PWM_CYCLE_TRIANGLE,

    /** Двойной проблеск ("маячок"): 12.5% вкл, 25% выкл, 25% вкл, 12.5%
     *  выкл, 25% пауза. Резкие прямоугольные фронты. */
    LOAD_PWM_CYCLE_DOUBLE_BLINK,

    /** Строб: пять коротких резких вспышек подряд, затем долгая пауза. */
    LOAD_PWM_CYCLE_STROBE,

    /** Служебное значение - количество определённых циклов. */
    LOAD_PWM_CYCLE_COUNT
} LOAD_PWM_Cycle_t;

/* ------------------------------------------------------------------------ */
/*  Режим работы нагрузки - внутреннее состояние, наружу не передаётся      */
/* ------------------------------------------------------------------------ */

typedef enum
{
    LOAD_PWM_MODE_NONE = 0,
    LOAD_PWM_MODE_REPEAT,
    LOAD_PWM_MODE_ONESHOT,
} LOAD_PWM_Mode_t;

typedef struct LOAD_PWM_Handle_s LOAD_PWM_Handle_t;

/* ------------------------------------------------------------------------ */
/*  Конфигурация для регистрации нагрузки                                   */
/* ------------------------------------------------------------------------ */

typedef struct
{
    /** Произвольный идентификатор "источника тика" - см. LOAD_PWM_Tick(). */
    uint32_t tick_source_id;

    /** Заявленная частота вызовов LOAD_PWM_Tick() с этим tick_source_id, Гц. */
    uint32_t tick_freq_hz;

    /** Таймер, к каналу которого физически подключена нагрузка по ШИМ. */
    TIM_HandleTypeDef *htim;

    /** Канал таймера (TIM_CHANNEL_1 и т.п.). */
    uint32_t channel;

    /** Тип нагрузки - см. LOAD_PWM_Type_t. */
    LOAD_PWM_Type_t load_type;

    /** Уровень скважности ШИМ (0..100% от ARR канала), нижняя граница
     *  рабочего диапазона (визуальный 0% для LED). */
    float brightness_min_percent;

    /** Только для LOAD_PWM_TYPE_LED: скважность ШИМ на визуальных ~50% -
     *  измеренная опорная точка для калибровки гамма-кривой. Отрицательное
     *  значение (например -1.0f) - использовать типовой
     *  LOAD_PWM_DEFAULT_LED_GAMMA вместо калибровки по факту. Для
     *  LOAD_PWM_TYPE_LINEAR игнорируется всегда. */
    float brightness_mid_percent;

    /** Уровень скважности ШИМ (0..100% от ARR канала), верхняя граница
     *  рабочего диапазона (визуальный 100% для LED). NaN на этом и
     *  остальных процентных полях безопасно трактуется как 0.0. */
    float brightness_max_percent;
} LOAD_PWM_Config_t;

/* ------------------------------------------------------------------------ */
/*  Хэндл нагрузки - главная структура API                                  */
/* ------------------------------------------------------------------------ */
/*
 * LOAD_PWM_Init() возвращает указатель НА ЭТУ структуру (память статическая,
 * живёт всё время работы программы). Поля объявлены целиком (без malloc),
 * но напрямую трогать их не нужно - только через API-функции. Поля
 * сгруппированы по размеру (4-байтовые, затем 1-байтовые) для минимизации
 * паддинга выравнивания - порядок объявления имеет значение, менять не
 * рекомендуется без переоценки занимаемой памяти.
 */
struct LOAD_PWM_Handle_s
{
    TIM_HandleTypeDef  *htim;
    uint32_t             tick_source_id;
    uint32_t             tick_freq_hz_cached;
    uint32_t             fade_ticks_remaining;
    volatile uint32_t    phase_acc;
    uint32_t              phase_inc;
    float                 fade_current_pwm;
    float                 fade_step;

    /* ---- Скважность ШИМ - тип зависит от LOAD_PWM_RAM_QUALITY ---- */
    LOAD_PWM_Duty_t      pwm_min;
    LOAD_PWM_Duty_t      pwm_max;
    LOAD_PWM_Duty_t      background_pwm;  /* целевое фоновое значение (логическое, без учёта LOAD_PWM_SetGlobalBrightness) */
    LOAD_PWM_Duty_t      current_pwm;     /* фактически выставленное сейчас (тоже логическое) */

    /* ---- Мелкие поля - намеренно сгруппированы вместе (упаковка) ---- */
    uint8_t              channel;
    uint8_t              cycle_id;         /* LOAD_PWM_Cycle_t, хранится как uint8_t */
    volatile uint8_t     mode;             /* LOAD_PWM_Mode_t, хранится как uint8_t - читается из Tick() */
    uint8_t               used;             /* слот занят (внутренняя бухгалтерия пула) */
    volatile uint8_t      fade_active;
    uint8_t                lut_index;        /* индекс в отдельном пуле LED LUT, либо LOAD_PWM_LUT_INDEX_NONE */
};

/* ------------------------------------------------------------------------ */
/*  Регистрация нагрузки                                                    */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Регистрирует нагрузку по заполненной конфигурации: пересчитывает
 *         границы диапазона из процентов в тики ШИМ (по текущему ARR
 *         канала, с насыщением до предела LOAD_PWM_Duty_t), для
 *         LOAD_PWM_TYPE_LED - выделяет слот в отдельном пуле LUT
 *         (LOAD_PWM_MAX_LEDS) и строит таблицу гамма-коррекции, запускает
 *         PWM канала (HAL_TIM_PWM_Start). Фоновая яркость по умолчанию - 0%.
 *
 * @note   Никакой "главный" таймер библиотека сама не запускает - см.
 *         LOAD_PWM_Tick() ниже, обеспечение периодичности вызова
 *         полностью на пользователе.
 *
 * @param  config  заполненная конфигурация (см. LOAD_PWM_Config_t)
 * @retval указатель на LOAD_PWM_Handle_t этой нагрузки, либо NULL при
 *         ошибке: config некорректен, исчерпан LOAD_PWM_MAX_LOADS, исчерпан
 *         LOAD_PWM_MAX_LEDS (для LOAD_PWM_TYPE_LED), либо не удалось
 *         запустить PWM канала (HAL_TIM_PWM_Start вернул не HAL_OK) - в этом
 *         случае слот нагрузки освобождается обратно в пул. Повторный вызов
 *         с тем же (htim, channel) идемпотентен - вернёт указатель на ТОТ ЖЕ
 *         хэндл, переиспользуя уже выделенный слот LUT, если он был.
 * @warning Если вызывается ПОВТОРНО для уже активной (тикающей) нагрузки не
 *          из того же контекста, что и её LOAD_PWM_Tick() - см. раздел
 *          "ПОТОКОБЕЗОПАСНОСТЬ" в начале файла.
 */
LOAD_PWM_Handle_t *LOAD_PWM_Init(const LOAD_PWM_Config_t *config);

/* ------------------------------------------------------------------------ */
/*  Фоновая яркость                                                          */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Задаёт фоновую яркость нагрузки (0..100%). Переход цикл <-> фон
 *         ВСЕГДА мгновенный, независимо от fade_ms. fade_ms управляет
 *         ТОЛЬКО плавностью перехода МЕЖДУ фоновыми значениями, когда не
 *         запущен ни один цикл (0 - немедленно; >0 - плавно за указанное
 *         время). Если цикл запущен - значение только запоминается,
 *         fade_ms игнорируется.
 *
 *         Для LOAD_PWM_TYPE_LINEAR percent преобразуется в тики ШИМ НАПРЯМУЮ
 *         (без промежуточного 8-битного "визуального" округления, которое
 *         используется только для индексации гамма-LUT у LED) - используется
 *         полное разрешение диапазона pwm_min..pwm_max независимо от
 *         LOAD_PWM_RAM_QUALITY. Для LOAD_PWM_TYPE_LED разрешение
 *         ограничено 256 шагами (это разрешение самой интерполяции по
 *         гамма-LUT, не искусственное урезание).
 * @param  h        хэндл нагрузки
 * @param  percent  0.0..100.0 (вне диапазона - обрезается)
 * @param  fade_ms  время плавного перехода, мс; 0 - немедленно
 * @retval HAL_OK; HAL_ERROR если h == NULL
 */
HAL_StatusTypeDef LOAD_PWM_SetBrightness(LOAD_PWM_Handle_t *h, float percent, uint32_t fade_ms);

/* ------------------------------------------------------------------------ */
/*  Управление циклами индикации                                            */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Запускает зацикленный (повторяющийся) цикл. Если тот же cycle
 *         УЖЕ запущен в зацикленном режиме - вызов ничего не делает
 *         (продолжает с текущей фазы). Иначе - стартует с нуля.
 * @param  h            хэндл нагрузки
 * @param  cycle         какой цикл запустить (см. LOAD_PWM_Cycle_t)
 * @param  phase_shift   сдвиг по фазе, доля периода 0.0..1.0
 * @param  period_ms     время выполнения одного полного цикла, мс (> 0)
 * @retval HAL_OK; HAL_ERROR если h == NULL, cycle вне диапазона либо
 *         period_ms == 0
 */
HAL_StatusTypeDef LOAD_PWM_Start(LOAD_PWM_Handle_t *h, LOAD_PWM_Cycle_t cycle,
                                  float phase_shift, uint32_t period_ms);

/**
 * @brief  Запускает цикл ОДИН РАЗ - по завершении отключается сам, ВСЕГДА
 *         стартует заново, даже если такой же цикл уже выполняется.
 * @param  h            хэндл нагрузки
 * @param  cycle         какой цикл запустить
 * @param  phase_shift   сдвиг по фазе (см. LOAD_PWM_Start)
 * @param  period_ms     время выполнения цикла, мс (> 0)
 * @retval HAL_OK; HAL_ERROR если h == NULL, cycle вне диапазона либо
 *         period_ms == 0
 */
HAL_StatusTypeDef LOAD_PWM_StartOnce(LOAD_PWM_Handle_t *h, LOAD_PWM_Cycle_t cycle,
                                      float phase_shift, uint32_t period_ms);

/**
 * @brief  Останавливает текущий цикл, немедленно возвращает к фоновому
 *         значению. Если ни один цикл не запущен - безопасно, ничего не
 *         меняет.
 * @param  h  хэндл нагрузки
 * @retval HAL_OK; HAL_ERROR если h == NULL
 */
HAL_StatusTypeDef LOAD_PWM_Stop(LOAD_PWM_Handle_t *h);

/**
 * @brief  Мгновенно останавливает циклы и возвращает к фоновой яркости ВСЕ
 *         зарегистрированные нагрузки одним вызовом (эквивалент
 *         LOAD_PWM_Stop для каждой).
 */
void LOAD_PWM_StopAll(void);

/* ------------------------------------------------------------------------ */
/*  Глобальный множитель яркости ("ночной режим")                           */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Задаёт глобальный множитель яркости (0..100%) для ВСЕХ нагрузок
 *         сразу - удобно для "ночного режима". Накладывается ПОСЛЕ гамма-
 *         коррекции, не искажая форму кривой; внутреннее логическое
 *         состояние множителем не затрагивается. Простаивающие нагрузки
 *         перерисовываются немедленно; нагрузки с активным циклом/фейдом
 *         подхватят множитель на ближайшем LOAD_PWM_Tick().
 * @param  percent  0.0..100.0 (100.0 - обычная яркость без затемнения)
 */
void LOAD_PWM_SetGlobalBrightness(float percent);

/* ------------------------------------------------------------------------ */
/*  Периодическое обновление                                                */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Продвигает активные циклы и плавные переходы фоновой яркости для
 *         ВСЕХ нагрузок, зарегистрированных с указанным tick_source_id.
 *         Вызывайте периодически с частотой, примерно равной заявленной
 *         tick_freq_hz - откуда угодно (таймер/main-loop/RTOS).
 *
 *         Максимально быстрая: без malloc, без деления в основной ветке,
 *         без блокировок и без вызова степенных/тригонометрических функций
 *         - только сложение, сравнение, сдвиг, табличная выборка и (для
 *         LED) одна линейная интерполяция по LUT, либо (для LINEAR) одно
 *         умножение по точной формуле. В обычном режиме (без активного
 *         фейда и без LOAD_PWM_SetGlobalBrightness < 100%) - ЦЕЛИКОМ
 *         целочисленная, ни одного float/double и ни одной программной
 *         soft-float операции даже на МК без аппаратного FPU (Cortex-M0/
 *         M0+, например STM32F030) - подтверждено дизассемблированием.
 *
 * @param  tick_source_id  идентификатор источника тика (см.
 *                          LOAD_PWM_Config_t.tick_source_id)
 * @warning Если вызывается ИЗ ПРЕРЫВАНИЯ - см. раздел "ПОТОКОБЕЗОПАСНОСТЬ" в
 *          начале файла про безопасное совмещение с вызовами остальных
 *          функций API из основного потока.
 */
void LOAD_PWM_Tick(uint32_t tick_source_id);

#ifdef __cplusplus
}
#endif

#endif /* STM32_LOAD_PWM_H */
