/**
 ******************************************************************************
 * @file    stm32_load_pwm.h
 * @brief   Библиотека управления ШИМ-нагрузкой на STM32: циклы индикации и
 *          гамма-коррекция для светодиодов, линейный режим для прочей
 *          нагрузки (лампы накаливания, кулеры, DC-моторы, электромагниты).
 * @author  Cload
 * @date    24.08.2026
 * @version 0.1
 *
 * @copyright Copyright (c) 2026 Cload.
 *            Свободное некоммерческое использование и модификация. Условия
 *            распространения - см. LICENSE / README.md в составе проекта.
 *
 *          Поддерживаемые платформы: STM32F4, STM32H7 (и, в целом, любой
 *          STM32 с периферией TIM и HAL). Специфичного для конкретной серии
 *          кода в библиотеке нет - используется только стандартный HAL TIM.
 *
 *          Проект переименован из stm32_led_pwm (24.08.2026, версия
 *          сохранена - 0.1, т.к. явного указания на переход к следующей
 *          версии не поступало) - изначально спроектированная под
 *          светодиоды архитектура (фазовый аккумулятор, циклы, гамма-
 *          коррекция) одинаково хорошо подходит для ЛЮБОЙ ШИМ-управляемой
 *          нагрузки, работающей по принципу "плавно/по циклу вкл-выкл", не
 *          только светодиодов.
 *
 *          === ИДЕЯ БИБЛИОТЕКИ ===
 *          Каждая нагрузка подключена к своему каналу ШИМ на своём таймере
 *          (могут быть разными у разных нагрузок - так быстрее и правильнее,
 *          чем гонять все нагрузки через общий таймер). Периодическое
 *          обновление скважности всех активных циклов делается вызовом
 *          LOAD_PWM_Tick(tick_source_id) - библиотека НЕ привязана к
 *          конкретному аппаратному "главному" таймеру: вызывайте эту
 *          функцию откуда угодно и с какой угодно периодичностью - из
 *          прерывания таймера, из тела while(1) в main(), из задачи RTOS.
 *          Единственное требование - реальная частота вызовов должна
 *          примерно соответствовать заявленной в конфигурации tick_freq_hz
 *          (иначе циклы будут идти быстрее/медленнее задуманного периода).
 *          tick_source_id - произвольный идентификатор, который вы сами
 *          придумываете для группировки: разные "источники тика" с разной
 *          частотой вызова (например часть нагрузок тикает из таймера
 *          на 1кГц, другая часть - из main-loop на ~100Гц) не мешают друг
 *          другу, если у них разные tick_source_id.
 *
 *          Каждая нагрузка регистрируется с явным ТИПОМ (LOAD_PWM_Type_t):
 *          - LOAD_PWM_TYPE_LED - гамма-коррекция (яркость светодиода
 *            воспринимается человеком нелинейно). Задаются опорные уровни
 *            скважности ШИМ (0..100% от ARR) для визуальных 0%, ~50% и
 *            100% яркости; библиотека подбирает степенную кривую
 *            u(x) = x^gamma, точно проходящую через все три точки. Если
 *            измерять/подбирать глазами среднюю точку не хочется -
 *            задайте brightness_mid_percent < 0 (маркер "не измерено") -
 *            тогда используется типовое значение LOAD_PWM_DEFAULT_LED_GAMMA
 *            (2.2 по умолчанию, стандартное приближение для светодиодов)
 *            вместо калибровки по факту - ценой чуть меньшей точности.
 *          - LOAD_PWM_TYPE_LINEAR - БЕЗ гамма-коррекции: скважность ШИМ
 *            линейна относительно заданного значения (0..100% между
 *            brightness_min_percent и brightness_max_percent напрямую).
 *            Для нагрузки, которая физически не требует компенсации
 *            восприятия глазом - лампа накаливания, кулер охлаждения,
 *            DC-мотор, электромагнит и т.п. Поле brightness_mid_percent в
 *            этом режиме не используется (игнорируется).
 *
 *          Форма каждого цикла индикации (меандр, дыхание, трапеция и т.д.)
 *          ЗАДАНА ТАБЛИЦЕЙ (LOAD_PWM_TABLE_SIZE точек на полный период,
 *          значения визуальной яркости 0..255), а не вычисляется формулой в
 *          рантайме - за один тик только сложение, сравнение, сдвиг и
 *          табличная выборка.
 *
 *          === КАК ПОЛЬЗОВАТЬСЯ (паттерн API) ===
 *          1. В CubeMX настраиваете таймер(ы) нагрузок: соответствующий
 *             канал в режиме PWM Generation, частота ШИМ - без видимого
 *             мерцания/жужжания (обычно от 1 кГц), ARR - как вам удобно
 *             (чем больше, тем плавнее управление). Отдельного "главного"
 *             таймера настраивать не нужно - см. выше.
 *          2. Заполняете LOAD_PWM_Config_t (включая tick_source_id и
 *             tick_freq_hz - на какой частоте вы сами обязуетесь вызывать
 *             LOAD_PWM_Tick с этим tick_source_id) и вызываете
 *             LOAD_PWM_Init(&config) - получаете указатель на
 *             LOAD_PWM_Handle_t. Он живёт всё время работы программы
 *             (указатель на статическую память модуля) - храните у себя.
 *          3. Организуете периодический вызов LOAD_PWM_Tick(tick_source_id)
 *             с заявленной частотой (таймер/main-loop/RTOS - на ваш выбор).
 *          4. Управляете нагрузкой через LOAD_PWM_SetBrightness (фоновая
 *             яркость, с опциональным плавным переходом) и LOAD_PWM_Start /
 *             LOAD_PWM_StartOnce / LOAD_PWM_Stop (циклы индикации), а также
 *             LOAD_PWM_StopAll() (аварийный сброс всех нагрузок разом) и
 *             LOAD_PWM_SetGlobalBrightness() (общий "ночной" множитель для
 *             всех нагрузок сразу).
 ******************************************************************************
 */

#ifndef STM32_LOAD_PWM_H
#define STM32_LOAD_PWM_H

#ifdef __cplusplus
extern "C" {
#endif

#include <stdint.h>
#include "main.h"   /* CubeMX: тянет за собой правильный stm32xxxx_hal.h с типом
                       TIM_HandleTypeDef, используемым во всех функциях ниже */

/* ------------------------------------------------------------------------ */
/*  Конфигурация модуля (define-ы, меняются до включения заголовка)         */
/* ------------------------------------------------------------------------ */

/** Максимальное число одновременно зарегистрированных нагрузок. */
#ifndef LOAD_PWM_MAX_LOADS
#define LOAD_PWM_MAX_LOADS              16U
#endif

/** Размер таблицы формы цикла (число точек на полный период) - степень
 *  двойки, чтобы выборка индекса из фазового аккумулятора была сдвигом без
 *  деления. LOAD_PWM_TABLE_SIZE = 2^LOAD_PWM_TABLE_BITS. */
#define LOAD_PWM_TABLE_BITS              7U
#define LOAD_PWM_TABLE_SIZE              (1U << LOAD_PWM_TABLE_BITS)

/** Таблица гамма-коррекции визуальная_яркость(0..255) -> ШИМ: 32 отрезка
 *  (33 узловые точки), каждый отрезок покрывает 256/32 = 8 значений
 *  визуальной яркости - поэтому индекс отрезка получается сдвигом. Для
 *  LOAD_PWM_TYPE_LINEAR та же таблица используется с gamma = 1 (линейна). */
#define LOAD_PWM_GAMMA_LUT_INDEX_SHIFT    3U
#define LOAD_PWM_GAMMA_LUT_SEGMENTS       32U
#define LOAD_PWM_GAMMA_LUT_POINTS         (LOAD_PWM_GAMMA_LUT_SEGMENTS + 1U)
#define LOAD_PWM_GAMMA_LUT_SEGMENTS_MASK  ((1U << LOAD_PWM_GAMMA_LUT_INDEX_SHIFT) - 1U)

/** Типовое значение gamma для LOAD_PWM_TYPE_LED, если пользователь не
 *  измерял точку 50% (см. brightness_mid_percent ниже). Переопределяемо. */
#ifndef LOAD_PWM_DEFAULT_LED_GAMMA
#define LOAD_PWM_DEFAULT_LED_GAMMA        2.2f
#endif

/* ------------------------------------------------------------------------ */
/*  Тип нагрузки                                                            */
/* ------------------------------------------------------------------------ */

typedef enum
{
    /** Светодиод - применяется гамма-коррекция (см. описание в начале
     *  файла и комментарий к LOAD_PWM_Init в .c). */
    LOAD_PWM_TYPE_LED = 0,

    /** Линейная нагрузка (лампа накаливания, кулер охлаждения, DC-мотор,
     *  электромагнит и т.п.) - скважность ШИМ линейна относительно
     *  заданного значения, БЕЗ гамма-коррекции. */
    LOAD_PWM_TYPE_LINEAR,
} LOAD_PWM_Type_t;

/* ------------------------------------------------------------------------ */
/*  Циклы индикации                                                         */
/* ------------------------------------------------------------------------ */
/*
 * Форма каждого цикла - готовая константная таблица в .c файле, заполняются
 * один раз на этапе компиляции. Каждая таблица описывает ОДИН полный период
 * (100% времени цикла).
 */
typedef enum
{
    /** Меандр: первая половина периода - максимум (визуально 100%), вторая
     *  половина - ноль (визуально 0%). */
    LOAD_PWM_CYCLE_MEANDR = 0,

    /** "Дыхание": один плавный горб на весь период - нарастание яркости от
     *  0% до 100% и плавный спад обратно до 0% (форма ~ sin по всему
     *  периоду), без пауз. */
    LOAD_PWM_CYCLE_BREATH,

    /** Импульс с ожиданием: в первой половине периода - плавное нарастание
     *  до 100% и спад до 0% (форма ~ sin), вторая половина периода -
     *  пауза на 0%. */
    LOAD_PWM_CYCLE_PULSE,

    /** Полутрапеция: 10% периода - линейное нарастание 0%->100%, 30% -
     *  полка на 100%, 10% - линейный спад 100%->0%, оставшиеся 50% -
     *  пауза на 0%. */
    LOAD_PWM_CYCLE_TRAPEZOID,

    /** Полупила: первая половина периода - линейное нарастание с 0% до
     *  100%, вторая половина - пауза на 0%. */
    LOAD_PWM_CYCLE_SAWTOOTH,

    /** Треугольник: непрерывное линейное нарастание 0%->100% и сразу без
     *  паузы линейный спад 100%->0% (в отличие от полупилы - без участка
     *  покоя, весь период в движении). */
    LOAD_PWM_CYCLE_TRIANGLE,

    /** Двойной проблеск ("маячок"): 12.5% периода включён на 100%, 25% -
     *  выключен, 25% - снова включён на 100%, 12.5% - выключен, оставшиеся
     *  25% периода - пауза. Резкие прямоугольные фронты. */
    LOAD_PWM_CYCLE_DOUBLE_BLINK,

    /** Строб: пять коротких резких вспышек на 100% подряд в первой части
     *  периода (~40%), дальше - долгая пауза. */
    LOAD_PWM_CYCLE_STROBE,

    /** Служебное значение - количество определённых циклов, не является
     *  валидным идентификатором цикла для передачи в функции API. */
    LOAD_PWM_CYCLE_COUNT
} LOAD_PWM_Cycle_t;

/* ------------------------------------------------------------------------ */
/*  Режим работы нагрузки - внутреннее состояние, наружу не передаётся      */
/* ------------------------------------------------------------------------ */

typedef enum
{
    LOAD_PWM_MODE_NONE = 0,   /* цикл не запущен - светит/работает фоновой яркостью */
    LOAD_PWM_MODE_REPEAT,     /* цикл запущен через LOAD_PWM_Start - зациклен  */
    LOAD_PWM_MODE_ONESHOT,    /* цикл запущен через LOAD_PWM_StartOnce - один раз */
} LOAD_PWM_Mode_t;

typedef struct LOAD_PWM_Handle_s LOAD_PWM_Handle_t;

/* ------------------------------------------------------------------------ */
/*  Конфигурация для регистрации нагрузки                                   */
/* ------------------------------------------------------------------------ */

typedef struct
{
    /** Произвольный идентификатор "источника тика" - вы сами придумываете
     *  число (например 0, 1, 2...) и им же вызываете LOAD_PWM_Tick(). Все
     *  нагрузки с одинаковым tick_source_id обновляются одним и тем же
     *  вызовом LOAD_PWM_Tick(). Не обязан быть связан ни с каким конкретным
     *  таймером - просто метка группировки. */
    uint32_t tick_source_id;

    /** Заявленная частота вызовов LOAD_PWM_Tick() с этим tick_source_id,
     *  Гц. Реальная периодичность вызова обеспечивается пользователем (из
     *  прерывания таймера, main-loop, задачи RTOS - не важно) и должна
     *  примерно соответствовать этому значению. */
    uint32_t tick_freq_hz;

    /** Таймер, к каналу которого физически подключена нагрузка по ШИМ.
     *  Уже должен быть настроен в CubeMX в режиме PWM Generation для
     *  указанного ниже канала (сам PWM Start выполнит библиотека). */
    TIM_HandleTypeDef *htim;

    /** Канал таймера, к которому подключена нагрузка (TIM_CHANNEL_1 и т.п.). */
    uint32_t channel;

    /** Тип нагрузки - см. LOAD_PWM_Type_t. */
    LOAD_PWM_Type_t load_type;

    /** Уровень скважности ШИМ (0..100% от ARR канала), соответствующий
     *  нижней границе рабочего диапазона (визуальный 0% для LED). */
    float brightness_min_percent;

    /** Только для LOAD_PWM_TYPE_LED: уровень скважности ШИМ, соответствующий
     *  визуальной яркости ~50% - измеренная вами опорная точка для
     *  калибровки гамма-кривой. Если измерять не хотите - задайте
     *  ОТРИЦАТЕЛЬНОЕ значение (например -1.0f) - тогда будет использован
     *  типовой LOAD_PWM_DEFAULT_LED_GAMMA вместо калибровки по факту.
     *  Для LOAD_PWM_TYPE_LINEAR это поле игнорируется всегда. */
    float brightness_mid_percent;

    /** Уровень скважности ШИМ (0..100% от ARR канала), соответствующий
     *  верхней границе рабочего диапазона (визуальный 100% для LED). */
    float brightness_max_percent;
} LOAD_PWM_Config_t;

/* ------------------------------------------------------------------------ */
/*  Хэндл нагрузки - главная структура API                                  */
/* ------------------------------------------------------------------------ */
/*
 * LOAD_PWM_Init() возвращает указатель НА ЭТУ структуру (память статическая,
 * живёт всё время работы программы - НЕ нужно её создавать/освобождать
 * самостоятельно, только хранить полученный указатель у себя).
 *
 * Все поля - ВНУТРЕННЕЕ состояние модуля. Структура объявлена целиком (а не
 * как непрозрачный тип) ради простоты и скорости (без malloc, без скрытых
 * аллокаций), но эти поля напрямую трогать не нужно - меняйте их только
 * через API-функции.
 */
struct LOAD_PWM_Handle_s
{
    /* ---- Привязка к периферии ---- */
    TIM_HandleTypeDef *htim;
    uint32_t            channel;
    uint32_t            tick_source_id;

    uint8_t             used;               /* слот занят (внутренняя бухгалтерия пула) */
    uint32_t            tick_freq_hz_cached; /* закэшированная частота тика этой нагрузки, Гц */

    /* ---- Пересчитанные из процентов опорные уровни ШИМ (в тиках CCR) ---- */
    uint32_t            pwm_min;
    uint32_t            pwm_mid;
    uint32_t            pwm_max;

    /** Таблица гамма-коррекции визуальная_яркость -> ШИМ (для
     *  LOAD_PWM_TYPE_LINEAR построена с gamma = 1, т.е. фактически линейна). */
    uint32_t            gamma_lut[LOAD_PWM_GAMMA_LUT_POINTS];

    /* ---- Фоновая яркость (когда ни один цикл не запущен) ---- */
    uint32_t            background_pwm;  /* целевое фоновое значение ШИМ (логическое, без учёта LOAD_PWM_SetGlobalBrightness) */
    uint32_t            current_pwm;     /* фактически выставленное сейчас значение ШИМ (тоже логическое) */

    /* ---- Плавный переход фоновой яркости (только между значениями фона,
     *      пока не запущен ни один цикл - см. LOAD_PWM_SetBrightness) ---- */
    volatile uint8_t    fade_active;
    float                fade_current_pwm;
    float                fade_step;
    uint32_t             fade_ticks_remaining;

    /* ---- Состояние активного цикла ---- */
    const uint8_t       *cycle_table;
    LOAD_PWM_Cycle_t     cycle_id;
    volatile LOAD_PWM_Mode_t mode;
    volatile uint32_t    phase_acc;
    uint32_t              phase_inc;
};

/* ------------------------------------------------------------------------ */
/*  Регистрация нагрузки                                                    */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Регистрирует нагрузку по заполненной конфигурации: пересчитывает
 *         опорные уровни из процентов в тики ШИМ (по текущему ARR канала),
 *         строит таблицу гамма-коррекции (для LOAD_PWM_TYPE_LINEAR - с
 *         gamma = 1), запускает PWM канала (HAL_TIM_PWM_Start). Фоновая
 *         яркость по умолчанию - 0%.
 *
 * @note   Никакой "главный" таймер библиотека сама не запускает - см.
 *         описание LOAD_PWM_Tick() ниже, обеспечение периодичности вызова
 *         полностью на пользователе.
 *
 * @param  config  заполненная конфигурация (см. LOAD_PWM_Config_t)
 * @retval указатель на LOAD_PWM_Handle_t этой нагрузки, либо NULL при
 *         ошибке: config некорректен (NULL, htim не задан, tick_freq_hz == 0),
 *         либо исчерпан LOAD_PWM_MAX_LOADS. Повторный вызов с тем же
 *         (htim, channel) идемпотентен - вернёт указатель на ТОТ ЖЕ хэндл.
 */
LOAD_PWM_Handle_t *LOAD_PWM_Init(const LOAD_PWM_Config_t *config);

/* ------------------------------------------------------------------------ */
/*  Фоновая яркость                                                          */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Задаёт фоновую яркость нагрузки (0..100%). Именно к этому значению
 *         нагрузка возвращается по остановке цикла (LOAD_PWM_Stop) или по
 *         завершению однократного цикла (LOAD_PWM_StartOnce) - этот переход
 *         ВСЕГДА мгновенный, без плавности, независимо от fade_ms.
 *
 *         Параметр fade_ms управляет ТОЛЬКО плавностью перехода МЕЖДУ
 *         фоновыми значениями, когда не запущен ни один цикл: fade_ms == 0
 *         - применить немедленно; fade_ms > 0 - плавно линейно за указанное
 *         время (повторный вызов посреди фейда плавно переопределяет цель).
 *         Если в этот момент цикл запущен - новое значение просто
 *         запоминается и применится мгновенно по остановке (fade_ms
 *         игнорируется).
 *
 * @param  h        хэндл нагрузки
 * @param  percent  0.0..100.0 (значения вне диапазона обрезаются)
 * @param  fade_ms  время плавного перехода, мс; 0 - немедленно
 * @retval HAL_OK; HAL_ERROR если h == NULL
 */
HAL_StatusTypeDef LOAD_PWM_SetBrightness(LOAD_PWM_Handle_t *h, float percent, uint32_t fade_ms);

/* ------------------------------------------------------------------------ */
/*  Управление циклами индикации                                            */
/* ------------------------------------------------------------------------ */
/*
 * Все функции неблокирующие и быстрые - выполняют только арифметику
 * настройки (без циклов ожидания, без блокировок), реальная отработка формы
 * цикла происходит позже, по вызовам LOAD_PWM_Tick().
 */

/**
 * @brief  Запускает зацикленный (повторяющийся) цикл. По окончании периода
 *         автоматически перезапускается заново - работает, пока не будет
 *         вызван LOAD_PWM_Stop() либо не будет запущен другой цикл.
 *
 *         Если указанный cycle УЖЕ запущен в зацикленном режиме - вызов
 *         НИЧЕГО не делает (продолжается с текущей фазы, phase_shift и
 *         period_ms игнорируются). Иначе - новый цикл стартует с нуля.
 *
 * @param  h            хэндл нагрузки
 * @param  cycle         какой цикл запустить (см. LOAD_PWM_Cycle_t)
 * @param  phase_shift   сдвиг по фазе, доля периода 0.0..1.0 (вне диапазона
 *                        берётся по модулю 1.0)
 * @param  period_ms     время выполнения одного полного цикла, мс (> 0)
 * @retval HAL_OK; HAL_ERROR если h == NULL, cycle вне диапазона либо
 *         period_ms == 0
 */
HAL_StatusTypeDef LOAD_PWM_Start(LOAD_PWM_Handle_t *h, LOAD_PWM_Cycle_t cycle,
                                  float phase_shift, uint32_t period_ms);

/**
 * @brief  Запускает цикл ОДИН РАЗ: по завершении периода автоматически
 *         отключается, яркость мгновенно возвращается к фоновой - в отличие
 *         от LOAD_PWM_Start, никогда не перезапускается сама и ВСЕГДА
 *         стартует заново, даже если такой же цикл уже выполняется.
 *
 * @param  h            хэндл нагрузки
 * @param  cycle         какой цикл запустить
 * @param  phase_shift   сдвиг по фазе (см. LOAD_PWM_Start)
 * @param  period_ms     время выполнения цикла, мс (> 0)
 * @retval HAL_OK; HAL_ERROR если h == NULL, cycle вне диапазона либо
 *         period_ms == 0
 */
HAL_StatusTypeDef LOAD_PWM_StartOnce(LOAD_PWM_Handle_t *h, LOAD_PWM_Cycle_t cycle,
                                      float phase_shift, uint32_t period_ms);

/**
 * @brief  Останавливает текущий цикл (Start или ещё не завершившийся
 *         StartOnce), если он был запущен - яркость немедленно возвращается
 *         к фоновому значению. Если ни один цикл не запущен - безопасно,
 *         ничего не меняет.
 * @param  h  хэндл нагрузки
 * @retval HAL_OK; HAL_ERROR если h == NULL
 */
HAL_StatusTypeDef LOAD_PWM_Stop(LOAD_PWM_Handle_t *h);

/**
 * @brief  Мгновенно останавливает циклы и возвращает к фоновой яркости ВСЕ
 *         зарегистрированные нагрузки одним вызовом (эквивалент LOAD_PWM_Stop
 *         для каждой). Пригодится для аварийного выключения/сброса в
 *         стартовое состояние (например по кнопке "паника" на транспорте).
 */
void LOAD_PWM_StopAll(void);

/* ------------------------------------------------------------------------ */
/*  Глобальный множитель яркости ("ночной режим")                           */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Задаёт глобальный множитель яркости (0..100%), применяемый КО
 *         ВСЕМ зарегистрированным нагрузкам сразу - удобно для "ночного
 *         режима" техники/установки без поштучного опроса и уменьшения
 *         фона/пределов каждой нагрузки. Накладывается уже ПОСЛЕ гамма-
 *         коррекции (на финальном значении ШИМ), не искажая форму кривой
 *         или активного цикла - внутреннее логическое состояние каждой
 *         нагрузки (фон, фаза цикла) множителем не затрагивается.
 *
 *         Простаивающие нагрузки (без активного цикла и без фейда)
 *         перерисовываются немедленно при вызове этой функции. Нагрузки с
 *         активным циклом или фейдом подхватят новый множитель на
 *         ближайшем вызове LOAD_PWM_Tick() для них (в пределах одного тика).
 *
 * @param  percent  0.0..100.0 (100.0 - обычная яркость без затемнения)
 */
void LOAD_PWM_SetGlobalBrightness(float percent);

/* ------------------------------------------------------------------------ */
/*  Периодическое обновление                                                */
/* ------------------------------------------------------------------------ */

/**
 * @brief  Продвигает активные циклы и плавные переходы фоновой яркости для
 *         ВСЕХ нагрузок, зарегистрированных с указанным tick_source_id, и
 *         записывает новое значение ШИМ в их каналы. Вызывайте периодически
 *         с частотой, примерно равной заявленной в конфигурации
 *         tick_freq_hz - откуда угодно: из HAL_TIM_PeriodElapsedCallback()
 *         своего таймера, из тела while(1) в main(), из задачи RTOS и т.п.
 *
 *         Максимально быстрая: без malloc, без деления в основной ветке,
 *         без блокировок и без вызова степенных/тригонометрических функций
 *         - только сложение, сравнение, сдвиг, табличная выборка и одна
 *         линейная интерполяция по таблице гамма-коррекции на нагрузку.
 *
 * @param  tick_source_id  идентификатор источника тика (см.
 *                          LOAD_PWM_Config_t.tick_source_id) - обновятся
 *                          только нагрузки, зарегистрированные с этим же id
 */
void LOAD_PWM_Tick(uint32_t tick_source_id);

#ifdef __cplusplus
}
#endif

#endif /* STM32_LOAD_PWM_H */
